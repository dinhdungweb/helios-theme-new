<div class="popup-overlay" id="popup">
  <div class="popup-content">
    <button class="popup-close" id="closePopup">&times;</button>
    <a href="https://elder.vn/collections/mua-2-tinh-tien-1">
    <img src="https://cdn.shopify.com/s/files/1/0633/2075/2319/files/pop_up.jpg?v=1741341301" alt=""/>
    </a>
  </div>
</div>
<style>
   .popup-overlay {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgb(0 0 0 / 81%);
      display: none; /* Ẩn mặc định */
      align-items: center;
      justify-content: center;
      z-index: 9999;
    }

    /* Popup chính */
    .popup-content {
      background: #fff;
      padding: 0px;
      border-radius: 8px;
      text-align: center;
      width: 480px;
      max-width: 100%;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      position: relative;
    }

    /* Nút tắt */
    .popup-close {
      position: absolute;
      top: 10px;
      right: 10px;
      background: #fff;
      color: #111;
      border: none;
      border-radius: 50%;
      width: 35px;
      height: 35px;
      display: flex;
      padding: 0;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      font-size: 25px!important;
    }

    .popup-close:hover {
      background: #ff3b30;
    }
</style>
<script>
  // JavaScript để xử lý popup
    document.addEventListener("DOMContentLoaded", function () {
      const popup = document.getElementById("popup");
      const closePopup = document.getElementById("closePopup");

      // Hiện popup sau 5 giây
      setTimeout(() => {
        popup.style.display = "flex"; // Hiện popup
      }, 3000);

      // Đóng popup khi nhấn nút
      closePopup.addEventListener("click", () => {
        popup.style.display = "none";
      });
    });
    // // JavaScript xử lý popup
    // document.addEventListener("DOMContentLoaded", function () {
    //   const popup = document.getElementById("popup");
    //   const closePopup = document.getElementById("closePopup");

    //   // Kiểm tra trạng thái popup trong localStorage
    //   const hasPopupShown = localStorage.getItem("popupShown");

    //   if (!hasPopupShown) {
    //     // Hiện popup nếu chưa được hiển thị
    //     setTimeout(() => {
    //       popup.style.display = "flex";
    //     }, 5000); // Hiện sau 5 giây
    //   }

    //   // Đóng popup và lưu trạng thái vào localStorage
    //   closePopup.addEventListener("click", () => {
    //     popup.style.display = "none";
    //     localStorage.setItem("popupShown", "true");
    //   });
    // });
  </script>